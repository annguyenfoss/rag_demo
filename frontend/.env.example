# =============================================================================
# Frontend Configuration Example
# =============================================================================
# Copy this file to .env or .env.local and update the values as needed
# This file can be committed to version control
# Actual .env files should NEVER be committed (they're in .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# Vite Development Server Configuration
# -----------------------------------------------------------------------------
# Note: Vite exposes environment variables prefixed with VITE_ to the client
# Variables without VITE_ prefix are only available during build time

# Development server port (default configured in vite.config.ts)
# Uncomment to override the default port of 9200
# VITE_DEV_PORT=9200

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# API base URL (default uses proxy in vite.config.ts: /api -> http://localhost:9100)
# Uncomment to override for production or different backend URL
# VITE_API_BASE_URL=http://localhost:9100

# Backend API proxy target (if you need to change from default)
# This would require modifying vite.config.ts
# Default: http://localhost:9100

